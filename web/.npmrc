# Hoisted linker: flatter node_modules without pnpm symlink trees.
# Needed for Vercel serverless packaging — isolated linker + monorepo trace
# triggers "invalid deployment package ... symlinked directories" during deploy.
node-linker=hoisted
